data/intake/*.sqlite3
data/intake/*.jsonl

# Pipeline — intermediate stage outputs (regenerated each run)
pipeline/logs/*.log
pipeline/output/raw_scraped.json
pipeline/output/_fixture_raw_scraped.json
pipeline/output/normalized.json
pipeline/output/llm_classified.json
pipeline/output/deduped.json
pipeline/output/classified.json
pipeline/output/validated.json
pipeline/output/metrics_*.json

# Keep: review_queue.json is committed so Mac Studio can serve it
# !pipeline/output/review_queue.json

# Backend (do not commit secrets or runtime artefacts)
backend/env
backend/venv/
backend/backups/
backend/__pycache__/

.DS_Store
**/.DS_Store

# PROCESS.ROOT.1 — regression guard run logs (live-prod evidence)
logs/regression-guard.*.json

# P5.AGENTS.1 — agent write lock (runtime, never committed)
.pc-write.lock
