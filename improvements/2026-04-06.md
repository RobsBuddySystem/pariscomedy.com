## 03:20 (Europe/Paris)
- **Task:** Daily comedians/current-shows maintenance pass.
- **Files reviewed:** `js/data.js`, `comedians.html`, `data/current-shows-review-queue.md`, `data/intake/submissions.sqlite3`, recent Mindi/standup notes.
- **What I checked:**
  - Verified the public comedians reference rules still point corrections/additions to Chuck.
  - Checked the local intake database for new comedian corrections/submissions (`submissions` count: `0`).
  - Reviewed the current review queue against available verification signals.
  - Re-checked one strong candidate: **Funny Women Paris** at **Le Noddi**.
- **What changed:**
  - Left the public current-shows layer unchanged in `js/data.js` because confidence was not high enough to publish a new verified entry without risking schedule drift.
  - Tightened the review-queue note for **Funny Women Paris** to record today’s evidence: a live Eventbrite page exists again, but the schedule fields still need a cleaner direct confirmation before publication.
- **Why this was the right move:**
  - The comedians page is a trust surface. Better to preserve a review-needed note than publish a shaky day/time and erode confidence.
- **Net result:**
  - **Public directory changes:** 0
  - **Review queue changes:** 1 note clarified
  - **New correction submissions processed:** 0

## 06:27 (Europe/Paris)
- **Reports read:** `dom-mkt-copywriter` daily (`2026-04-06.md`), `hal` daily (`2026-04-06.md`)
- **Task picked (highest revenue impact):** P0 trust cleanup — remove internal ops/agent language from public homepage and shows-page copy so customers see audience-facing messaging instead of backstage process notes.
- **What I changed:**
  - Rewrote public-facing badges/helpers on `index.html` and `shows.html` that exposed internal references like “Vision insight,” “From Dom Copywriter,” “Alex's promo note,” and “Analyst note.”
  - Kept the same conversion intent and structure, but translated the copy into customer language focused on identity, trust, and easy show selection.
  - Preserved the expat/tourist positioning from Dom Copywriter's report while removing anything that made the site feel like an internal ops board.
- **Validation:**
  - Local `curl http://127.0.0.1:8765/index.html | grep` confirmed the new audience-facing copy (`Why this works: clear identity, fast booking, real proof`, `A bilingual hook that clicks with expats and tourists`).
  - Local `curl http://127.0.0.1:8765/shows.html | grep` confirmed the new trust-safe helper copy (`pick the neighborhood first, then the ticket`, `fast, obvious choice instead of guesswork`).
  - Deployed to `main`: commit `9e09023`.
- **Revenue impact:** Cleans up a credibility leak right on high-intent pages. Visitors now see polished consumer copy instead of internal agent/process language, which should improve trust at the exact moment they decide whether to click through to Eventbrite.

## 07:06 (Europe/Paris)
- **Reports read:** `dom-trend-scout` daily (`2026-04-06.md`), `betty` daily (`2026-04-06.md`)
- **Task picked (highest revenue impact):** P0 trust fix — correct Green Mic's verified public venue details and live showtimes so Friday/Sunday visitors click into accurate listings instead of fuzzy venue info.
- **What I changed:**
  - Updated `js/data.js` with verified Eventbrite details for **Green Mic Showcase** at **Au Soleil de la Butte, 32 Rue Muller, 75018 Paris**.
  - Corrected the Friday showcase time from **20:15 → 20:30** (doors 20:15) and the Sunday Green Mic show from **19:15 → 19:30** with the verified address **5 Rue du Faubourg Montmartre, 75009 Paris**.
  - Updated the Green Mic schema/address references on `shows.html` and `venues.html` so search engines and venue-map users get the same verified data.
- **Validation:**
  - Local `curl http://127.0.0.1:8765/shows.html | grep` confirmed `Friday at 20:30`, `19:30 — Green Mic Comedy Show`, and `32 Rue Muller`.
  - Local `curl http://127.0.0.1:8765/venues.html | grep` confirmed Green Mic now renders with the verified Montmartre street address in schema.
  - Deployed to `main`: commit `edc78f5`.
- **Revenue impact:** This fixes a trust-breaking mismatch on public listing pages. Accurate venue/time data reduces drop-off for high-intent Friday/Sunday visitors, makes the venue map more credible, and improves the odds that Green Mic traffic turns into real reservations instead of confusion.

## 07:22 (Europe/Paris)
- **Task:** Daily comedians/current-shows maintenance pass.
- **Files reviewed:** `js/data.js`, `comedians.html`, `data/current-shows-review-queue.md`, `data/intake/submissions.sqlite3`, repo history for earlier Mindi verification commits.
- **What I checked:**
  - Confirmed correction routing still points comics to Chuck via `book.html#contactForm` from the public comedians page.
  - Checked the local intake database again: `submissions` count is still `0`.
  - Re-checked review-queue candidates conservatively after web search failed behind bot detection.
  - Used one direct live fetch for a previously known URL to avoid guessing: **Funny Women Paris** on Eventbrite.
- **What changed:**
  - Promoted **Funny Women Paris** into `OTHER_SHOWS_RAW` in `js/data.js` after a direct Eventbrite fetch confirmed a live multi-date listing at **Le Noddi, 16 Rue Bernardins, 75005 Paris** with **doors 20:00 / show 20:30** and Tuesday recurrence.
  - Updated the Le Noddi venue description from **20:00 → 20:30** to match the live listing.
  - Removed Funny Women from `data/current-shows-review-queue.md` and added a promotion note documenting why it was safe to publish.
  - Left the remaining uncertain candidates in manual review instead of backfilling from stale repo memory.
- **Why this was the right move:**
  - This pass improved the public reference with one confidently verified show while keeping the trust standard intact for everything else.
- **Net result:**
  - **Public directory changes:** 1 verified show added
  - **Review queue changes:** 1 item removed from review, promotion note added
  - **New correction submissions processed:** 0

## 07:27 (Europe/Paris)
- **Reports read:** `vision` daily (`2026-04-06.md`), `mitzi` daily (`2026-04-06.md`)
- **Task picked (highest revenue impact):** P1 revenue lane — add a homepage Sunday booking section that turns under-monetized Sunday inventory into an obvious conversion path for tourists, expats, date-night visitors, and room operators.
- **What I changed:**
  - Added a new homepage `Sunday in Paris` booking lane directly under the identity-first starter pack.
  - Gave visitors two verified Sunday choices with one-click CTAs: **Oscar Comedy Club (17:00)** and **Green Mic Comedy Show (19:30)**.
  - Added a third CTA for room operators/private events to push Sunday booking demand toward `book.html`, using Mitzi's note that Sunday inventory is still the softest commercial lane.
  - Framed the copy around Vision's identity-first conversion rule: make the premise obvious, keep the choice simple, and shorten the path from vibe → ticket.
- **Validation:**
  - Local curl confirmed the homepage now renders `Sunday in Paris: easiest room to book this week`, `Reserve Oscar`, and `Reserve Green Mic`.
  - Mobile check confirmed the three-card lane stacks cleanly through the existing `starter-pack-grid` responsive layout.
- **Revenue impact:** Surfaces a previously under-owned booking lane right on the homepage, giving Sunday shows a dedicated conversion path instead of forcing users to dig through listings. That should increase Sunday reservations and create a clearer upsell path for private/corporate Sunday bookings.
